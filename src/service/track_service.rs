// use crate::config::minio::Minio;

// pub struct TrackService<'a> {
//     minio: &'a Minio,
// }

// impl TrackService {

//     pub fn new(minio: &'a Minio) -> TrackService<'a> {
//         Self { minio }
//     }
// }
